__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
ENV/
.DS_Store
*.egg-info/
dist/
build/
sim_backup/
.claude/

# Internal working notes — session handoffs and downloaded reference PDFs
# (the official rules PDF is linked from the README instead of committed).
docs/handoffs/
docs/reference/
files/

# Local CAD/measurement reference (screenshots etc.)
models/reference/
# Oversized experimental assemblies (61 MB / 41 MB) — kept local only
models/print/New assembly test.stl
models/cad-source/New assembly test.STEP
# Printer-sliced output (machine-specific; STLs are the shareable artifact)
*.gcode.3mf

# Robot credentials / host config — copy .env.robot.example and fill in locally.
.env
.env.robot

# Reference clones of other repos (not ours to track)
external/

# Full-res screen-recording originals stay local; compressed .mp4s are tracked
video/*.mov

# Journal drafts stay local until final (add the final PDF deliberately)
ARTEMIS DOC*.pdf
