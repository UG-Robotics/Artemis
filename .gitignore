__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
ENV/
.DS_Store
*.egg-info/
dist/
build/
screenshots/
sim_backup/
*.pdf
*.docx
*.jpeg
*.jpg
*.zip
files/
docs/
.claude/

# CAD / 3D-model binaries — kept local only (organised under models/).
# Large; source lives in the team's CAD tool, not git.
*.stl
*.STL
*.step
*.STEP
*.stp
*.STP
*.sldprt
*.SLDPRT
*.3mf
*.svg
# Local CAD/measurement reference (screenshots etc.)
models/reference/

# Robot credentials / host config — copy .env.robot.example and fill in locally.
.env
.env.robot

# Reference clones of other repos (not ours to track)
external/

# Full-res screen-recording originals stay local; compressed .mp4s are tracked
video/*.mov
