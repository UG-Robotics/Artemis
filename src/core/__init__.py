"""Shared control brain for Artemis.

Config constants, the `SensorReading` contract, and the `Controller`. Both the
sim and the real robot import from here, so the control logic lives in one place.
"""
